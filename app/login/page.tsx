import LoginForm from "@/app/features/auth/components/LoginForm";

/**
 * Composes the login route.
 */
export default function LoginPage() {
    return (
        <main className="authLayout">
            <LoginForm />
        </main>
    );
}
